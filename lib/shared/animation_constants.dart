import 'package:flutter/animation.dart';

const kCardFlipDuration = Duration(milliseconds: 150);
const kCardMoveDuration = Duration(milliseconds: 200);
const kCardDropDuration = Duration(milliseconds: 120);
const kCardAutoMoveDuration = Duration(milliseconds: 220);
const kCardDealDuration = Duration(milliseconds: 280);
const kCardHighlightDuration = Duration(milliseconds: 180);
const kCardOverlayDuration = Duration(milliseconds: 160);
const kWinAnimationDuration = Duration(milliseconds: 1500);
const kWinSparkleStepDuration = Duration(milliseconds: 120);

const kBoardPieceMoveDuration = Duration(milliseconds: 220);
const kBoardPieceCaptureDuration = Duration(milliseconds: 150);
const kHintPulseDuration = Duration(milliseconds: 200);
const kHintHighlightDuration = Duration(milliseconds: 180);
const kAiMoveDelay = Duration(milliseconds: 220);

const kSudokuHintDuration = Duration(milliseconds: 300);
const kTileSlideDuration = Duration(milliseconds: 150);
const kTileMergePopDuration = Duration(milliseconds: 100);
const kMinesweeperRevealDuration = Duration(milliseconds: 50);
